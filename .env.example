# Copy this file to .env and fill in the values you need. .env is gitignored.

# --- App ---
# APP_NAME=Your App
# SECRET_KEY=change-me-in-production

# --- Database ---
# SQLite only. Optional: override the file path (defaults to app.db next to
# this file — each provisioned instance gets its own).
# SQLITE_PATH=app.db

# --- Storage ---
# Attachments are stored on local disk. Optional: override where they live
# (defaults to ./uploads next to this file).
# UPLOAD_FOLDER=/path/to/uploads

# --- AI backend (Ask AI and Capture) ---
# Two interchangeable backends, shared by the chat agent and by Capture's
# text-to-records extraction (see ai.py). Without one of these, every other
# page works normally and Capture says so plainly instead of failing.
#   - OpenAI-compatible cloud: set OPENAI_API_KEY.
#   - Local Ollama: leave OPENAI_API_KEY unset (the default).
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=gemma4

# --- Email (Resend), used for invite + password-reset emails ---
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# RESEND_FROM=no-reply@example.com

# --- Dev server ---
# HOST=0.0.0.0
# PORT=8000
# DEBUG=1
